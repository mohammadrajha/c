import 'package:flutter/material.dart';

class ConfigLang {
  static const String langPath = 'assets/lang'; // without backslash
  static const Locale enLocale = Locale('en', 'US');
  static const Locale arLocale = Locale('ar', 'JO');
}
